/* TODO:
    - Make generateRuleString() take a list of rules (in what form??) as parameters and generate a custom ruleset
    - Add UI for custom rule generation
*/

// TODO: Add UI control for grid size
